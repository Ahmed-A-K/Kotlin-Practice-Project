fun main() {
    //var variables are mutable.
    var name:String = "Ahmed"
    name = "Ahmed Ali Khan"

    //val variables are not mutable. They are read-only once declared and assigned.
    val age:Int = 19;
    // age = 20; this code gives an error indicating that age cannot be mutated or changed
}